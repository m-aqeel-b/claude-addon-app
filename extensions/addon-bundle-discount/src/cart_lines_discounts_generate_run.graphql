query CartInput {
  cart {
    lines {
      id
      quantity
      cost {
        amountPerQuantity {
          amount
        }
      }
      addonBundleId: attribute(key: "_addon_bundle_id") {
        value
      }
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            id
          }
        }
      }
      sellingPlanAllocation {
        sellingPlan {
          id
        }
      }
    }
  }
  discount {
    metafield(namespace: "$app:addon-bundle", key: "config") {
      value
    }
    discountClasses
  }
}
